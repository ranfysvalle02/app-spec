-- {{ spec.app_name }} — Seed Data
-- Auto-generated by AppSpec v{{ spec.schema_version }}
--
-- {{ spec.sample_data | length }} tables seeded

{% if spec.sample_data %}
{% for collection, docs in spec.sample_data.items() %}
{% set entity = spec.entities | selectattr("collection", "equalto", collection) | first %}
{% if entity and docs %}
{%- set seed_fields = [] %}
{%- for field in entity.fields %}
{%-   if field.name in docs[0] %}
{%-     set _ = seed_fields.append(field) %}
{%-   elif field.required and field.type == "reference" and field.reference and field.reference in spec.sample_data and spec.sample_data[field.reference] %}
{%-     set _ = seed_fields.append(field) %}
{%-   endif %}
{%- endfor %}
-- ── {{ entity.name }} ({{ docs | length }} records) ──

{% for doc in docs %}
{%- set doc_idx = loop.index0 %}
INSERT INTO {{ collection }} (
{% for field in seed_fields %}
    {{ field.name }}{{ "," if not loop.last else "" }}
{% endfor %}
) VALUES (
{% for field in seed_fields %}
{%-   if field.name in doc %}
    {{ db.sql_value_literal(doc[field.name], field) }}{{ "," if not loop.last else "" }}
{%-   elif field.type == "reference" and field.reference and field.reference in spec.sample_data %}
{%-     set ref_docs = spec.sample_data[field.reference] %}
{%-     set ref_doc = ref_docs[doc_idx % (ref_docs | length)] %}
    {{ db.sql_value_literal(ref_doc.get("id", ref_doc.get("_id")), field) }}{{ "," if not loop.last else "" }}
{%-   else %}
    NULL{{ "," if not loop.last else "" }}
{%-   endif %}
{% endfor %}
);
{% endfor %}

{% endif %}
{% endfor %}
{% else %}
-- No sample data provided — app starts empty.
{% endif %}
