/**
 * {{ spec.app_name }} — API Routes
 *
 * Auto-generated by AppSpec v{{ spec.schema_version }}.
 */

import { Router, Request, Response } from "express";
import mongoose from "mongoose";
{% for entity in spec.entities %}
import { {{ entity.name }} } from "./models";
{% endfor %}
{% if spec.auth.enabled %}
import { authenticate{% if spec.auth.roles %}, authorize{% endif %}, AuthRequest } from "./auth";
{% endif %}

export const router = Router();

{% for entity in spec.entities %}
// ── {{ entity.name }} ──────────────────────────────────────────

{% set ep_list = spec.endpoints | selectattr("entity", "equalto", entity.name) | selectattr("operation.value", "equalto", "list") | list %}
{% set ep_get = spec.endpoints | selectattr("entity", "equalto", entity.name) | selectattr("operation.value", "equalto", "get") | list %}
{% set ep_create = spec.endpoints | selectattr("entity", "equalto", entity.name) | selectattr("operation.value", "equalto", "create") | list %}
{% set ep_update = spec.endpoints | selectattr("entity", "equalto", entity.name) | selectattr("operation.value", "equalto", "update") | list %}
{% set ep_delete = spec.endpoints | selectattr("entity", "equalto", entity.name) | selectattr("operation.value", "equalto", "delete") | list %}

{% if ep_list %}
{% set ep = ep_list[0] %}
router.get("/{{ entity.collection }}",
{% if spec.auth.enabled and ep.auth_required %}
  authenticate,
{% if ep.roles %}
  authorize({{ ep.roles | map("tojson") | join(", ") }}),
{% endif %}
{% endif %}
  async (req: Request, res: Response) => {
  try {
    const skip = Math.max(0, parseInt(req.query.skip as string) || 0);
    const limit = Math.min(200, Math.max(1, parseInt(req.query.limit as string) || 50));
    const items = await {{ entity.name }}.find().select("{% for field in entity.fields %}{{ field.name }} {% endfor %}createdAt updatedAt").sort({ _id: -1 }).skip(skip).limit(limit);
    res.json(items);
  } catch (err: any) {
    res.status(500).json({ error: err.message });
  }
});
{% endif %}

{% if ep_get %}
{% set ep = ep_get[0] %}
router.get("/{{ entity.collection }}/:id",
{% if spec.auth.enabled and ep.auth_required %}
  authenticate,
{% if ep.roles %}
  authorize({{ ep.roles | map("tojson") | join(", ") }}),
{% endif %}
{% endif %}
  async (req: Request, res: Response) => {
  try {
    if (!mongoose.Types.ObjectId.isValid(req.params.id)) {
      res.status(400).json({ error: "Invalid ID format" });
      return;
    }
    const item = await {{ entity.name }}.findById(req.params.id);
    if (!item) {
      res.status(404).json({ error: "{{ entity.name }} not found" });
      return;
    }
    res.json(item);
  } catch (err: any) {
    res.status(500).json({ error: err.message });
  }
});
{% endif %}

{% if ep_create %}
{% set ep = ep_create[0] %}
router.post("/{{ entity.collection }}",
{% if spec.auth.enabled and ep.auth_required %}
  authenticate,
{% if ep.roles %}
  authorize({{ ep.roles | map("tojson") | join(", ") }}),
{% endif %}
{% endif %}
  async (req: Request, res: Response) => {
  try {
    const item = await {{ entity.name }}.create(req.body);
    res.status(201).json(item);
  } catch (err: any) {
    if (err.name === "ValidationError") {
      res.status(400).json({ error: err.message });
      return;
    }
    res.status(500).json({ error: err.message });
  }
});
{% endif %}

{% if ep_update %}
{% set ep = ep_update[0] %}
router.put("/{{ entity.collection }}/:id",
{% if spec.auth.enabled and ep.auth_required %}
  authenticate,
{% if ep.roles %}
  authorize({{ ep.roles | map("tojson") | join(", ") }}),
{% endif %}
{% endif %}
  async (req: Request, res: Response) => {
  try {
    if (!mongoose.Types.ObjectId.isValid(req.params.id)) {
      res.status(400).json({ error: "Invalid ID format" });
      return;
    }
    const item = await {{ entity.name }}.findByIdAndUpdate(req.params.id, req.body, { new: true, runValidators: true });
    if (!item) {
      res.status(404).json({ error: "{{ entity.name }} not found" });
      return;
    }
    res.json(item);
  } catch (err: any) {
    if (err.name === "ValidationError") {
      res.status(400).json({ error: err.message });
      return;
    }
    res.status(500).json({ error: err.message });
  }
});
{% endif %}

{% if ep_delete %}
{% set ep = ep_delete[0] %}
router.delete("/{{ entity.collection }}/:id",
{% if spec.auth.enabled and ep.auth_required %}
  authenticate,
{% if ep.roles %}
  authorize({{ ep.roles | map("tojson") | join(", ") }}),
{% endif %}
{% endif %}
  async (req: Request, res: Response) => {
  try {
    if (!mongoose.Types.ObjectId.isValid(req.params.id)) {
      res.status(400).json({ error: "Invalid ID format" });
      return;
    }
    const result = await {{ entity.name }}.findByIdAndDelete(req.params.id);
    if (!result) {
      res.status(404).json({ error: "{{ entity.name }} not found" });
      return;
    }
    res.json({ ok: true });
  } catch (err: any) {
    res.status(500).json({ error: err.message });
  }
});
{% endif %}

{% endfor %}
