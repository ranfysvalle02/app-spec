"""{{ spec.app_name }} — Database Connection

Auto-generated by AppSpec v{{ spec.schema_version }}.
"""

import os

from pymongo import AsyncMongoClient

_client: AsyncMongoClient | None = None
_db = None


async def connect_db():
    global _client, _db
    uri = os.getenv("MONGODB_URI", "mongodb://localhost:27017")
    db_name = os.getenv("MONGODB_DB", "{{ spec.slug | replace("-", "_") }}")
    _client = AsyncMongoClient(uri)
    _db = _client[db_name]


async def close_db():
    global _client
    if _client:
        await _client.close()


def get_db():
    if _db is None:
        raise RuntimeError("Database not connected. Call connect_db() first.")
    return _db
