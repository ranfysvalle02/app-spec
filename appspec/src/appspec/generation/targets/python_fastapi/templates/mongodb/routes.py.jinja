"""{{ spec.app_name }} — API Routes

Auto-generated by AppSpec v{{ spec.schema_version }}.
"""

from datetime import datetime, timezone

from bson import ObjectId
from fastapi import APIRouter, HTTPException, Query
{% if spec.auth.enabled %}
from fastapi import Depends
from auth import get_current_user{% if spec.auth.roles %}, require_roles{% endif %}

{% endif %}

from database import get_db
from models import (
{% for entity in spec.entities %}
    {{ entity.name }}Create,
    {{ entity.name }}Response,
{% endfor %}
)

router = APIRouter()


def _oid(id_str: str) -> ObjectId:
    try:
        return ObjectId(id_str)
    except Exception:
        raise HTTPException(400, "Invalid ID format")


def _doc_to_response(doc: dict) -> dict:
    if not doc:
        return doc
    for key, value in doc.items():
        if isinstance(value, ObjectId):
            doc[key] = str(value)
    return doc


{% for entity in spec.entities %}
# ── {{ entity.name }} ──────────────────────────────────────────

{% set ep_list = spec.endpoints | selectattr("entity", "equalto", entity.name) | selectattr("operation.value", "equalto", "list") | list %}
{% set ep_get = spec.endpoints | selectattr("entity", "equalto", entity.name) | selectattr("operation.value", "equalto", "get") | list %}
{% set ep_create = spec.endpoints | selectattr("entity", "equalto", entity.name) | selectattr("operation.value", "equalto", "create") | list %}
{% set ep_update = spec.endpoints | selectattr("entity", "equalto", entity.name) | selectattr("operation.value", "equalto", "update") | list %}
{% set ep_delete = spec.endpoints | selectattr("entity", "equalto", entity.name) | selectattr("operation.value", "equalto", "delete") | list %}

{% if ep_list %}
{% set ep = ep_list[0] %}
@router.get("/{{ entity.collection }}", response_model=list[{{ entity.name }}Response])
async def list_{{ entity.collection }}(
    skip: int = Query(0, ge=0),
    limit: int = Query(50, ge=1, le=200),
{% if spec.auth.enabled and ep.auth_required %}
{% if ep.roles %}
    current_user: dict = Depends(require_roles({{ ep.roles | map("tojson") | join(", ") }})),
{% else %}
    current_user: dict = Depends(get_current_user),
{% endif %}
{% endif %}
):
    db = get_db()
    projection = {
{% for field in entity.fields %}
        "{{ field.name }}": 1,
{% endfor %}
        "created_at": 1,
        "updated_at": 1,
    }
    cursor = db["{{ entity.collection }}"].find({}, projection).skip(skip).limit(limit).sort("_id", -1)
    return [_doc_to_response(doc) async for doc in cursor]

{% endif %}
{% if ep_get %}
{% set ep = ep_get[0] %}
@router.get("/{{ entity.collection }}/{item_id}", response_model={{ entity.name }}Response)
async def get_{{ entity.collection | replace("-", "_") }}_by_id(
    item_id: str,
{% if spec.auth.enabled and ep.auth_required %}
{% if ep.roles %}
    current_user: dict = Depends(require_roles({{ ep.roles | map("tojson") | join(", ") }})),
{% else %}
    current_user: dict = Depends(get_current_user),
{% endif %}
{% endif %}
):
    db = get_db()
    doc = await db["{{ entity.collection }}"].find_one({"_id": _oid(item_id)})
    if not doc:
        raise HTTPException(404, "{{ entity.name }} not found")
    return _doc_to_response(doc)

{% endif %}
{% if ep_create %}
{% set ep = ep_create[0] %}
@router.post("/{{ entity.collection }}", response_model={{ entity.name }}Response, status_code=201)
async def create_{{ entity.collection | replace("-", "_") }}(
    data: {{ entity.name }}Create,
{% if spec.auth.enabled and ep.auth_required %}
{% if ep.roles %}
    current_user: dict = Depends(require_roles({{ ep.roles | map("tojson") | join(", ") }})),
{% else %}
    current_user: dict = Depends(get_current_user),
{% endif %}
{% endif %}
):
    db = get_db()
    doc = data.model_dump()
    doc["created_at"] = datetime.now(timezone.utc)
    doc["updated_at"] = doc["created_at"]
    result = await db["{{ entity.collection }}"].insert_one(doc)
    doc["_id"] = str(result.inserted_id)
    return doc

{% endif %}
{% if ep_update %}
{% set ep = ep_update[0] %}
@router.put("/{{ entity.collection }}/{item_id}", response_model={{ entity.name }}Response)
async def update_{{ entity.collection | replace("-", "_") }}(
    item_id: str,
    data: {{ entity.name }}Create,
{% if spec.auth.enabled and ep.auth_required %}
{% if ep.roles %}
    current_user: dict = Depends(require_roles({{ ep.roles | map("tojson") | join(", ") }})),
{% else %}
    current_user: dict = Depends(get_current_user),
{% endif %}
{% endif %}
):
    db = get_db()
    update = {"$set": {**data.model_dump(), "updated_at": datetime.now(timezone.utc)}}
    result = await db["{{ entity.collection }}"].find_one_and_update(
        {"_id": _oid(item_id)}, update, return_document=True
    )
    if not result:
        raise HTTPException(404, "{{ entity.name }} not found")
    return _doc_to_response(result)

{% endif %}
{% if ep_delete %}
{% set ep = ep_delete[0] %}
@router.delete("/{{ entity.collection }}/{item_id}")
async def delete_{{ entity.collection | replace("-", "_") }}(
    item_id: str,
{% if spec.auth.enabled and ep.auth_required %}
{% if ep.roles %}
    current_user: dict = Depends(require_roles({{ ep.roles | map("tojson") | join(", ") }})),
{% else %}
    current_user: dict = Depends(get_current_user),
{% endif %}
{% endif %}
):
    db = get_db()
    result = await db["{{ entity.collection }}"].delete_one({"_id": _oid(item_id)})
    if result.deleted_count == 0:
        raise HTTPException(404, "{{ entity.name }} not found")
    return {"ok": True}

{% endif %}
{% endfor %}
